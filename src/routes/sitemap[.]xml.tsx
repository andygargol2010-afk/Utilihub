import{createFileRoute}from"@tanstack/react-router";import{CATEGORIES,TOOLS}from"@/lib/tools";const BASE_URL="https://utilihub.vercel.app";export const Route=createFileRoute("/sitemap.xml")({server:{handlers:{GET:()=>{const paths=["/","/herramientas",...CATEGORIES.map(c=>`/categoria/${c.slug}`),...TOOLS.map(t=>`/herramientas/${t.slug}`)];const xml=`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${paths.map(p=>`<url><loc>${BASE_URL}${p}</loc></url>`).join("")}</urlset>`;return new Response(xml,{headers:{"Content-Type":"application/xml; charset=utf-8"}})}}}});