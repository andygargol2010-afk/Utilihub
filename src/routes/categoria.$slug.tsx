import{createFileRoute,notFound}from"@tanstack/react-router";import{Breadcrumbs}from"@/components/Breadcrumbs";import{ToolCard}from"@/components/ToolCard";import{categoryBySlug,toolsByCategory,type CategorySlug}from"@/lib/tools";export const Route=createFileRoute("/categoria/$slug")({loader:({params})=>{const category=categoryBySlug(params.slug);if(!category)throw notFound();return{category,tools:toolsByCategory(category.slug as CategorySlug)}},head:({loaderData})=>loaderData?{meta:[{title:loaderData.category.title},{name:"description",content:loaderData.category.description}],links:[{rel:"canonical",href:`/categoria/${loaderData.category.slug}`}] }:{meta:[{title:"Categoría no encontrada | UtiliHub"},{name:"robots",content:"noindex"}]},component:CategoryPage});function CategoryPage(){const{category,tools}=Route.useLoaderData();return <div className="container-page py-10"><Breadcrumbs items={[{label:"Inicio",to:"/"},{label:"Herramientas",to:"/herramientas"},{label:category.name}]}/><h1 className="mt-4 text-3xl font-bold sm:text-4xl">{category.name}</h1><p className="mt-3 max-w-2xl text-muted-foreground">{category.intro}</p><div className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{tools.map(t=><ToolCard key={t.slug} tool={t}/>)}</div></div>}