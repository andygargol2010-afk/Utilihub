export type GeneralCategory={slug:string;name:string;title:string;description:string;intro:string};
export type GeneralTool={slug:string;name:string;title:string;description:string;summary:string;category:string;keywords:string[];about:string[];steps:string[];faq:{q:string;a:string}[];kind:string;config?:Record<string,unknown>};
export const faq=(q:string,a:string)=>({q,a});
export const makeTool=(slug:string,name:string,category:string,kind:string,summary:string,keywords:string[],config:Record<string,unknown>={})=>({slug,name,title:`${name} online gratis | UtiliHub`,description:`${summary} Gratis, sin registro y con procesamiento local cuando es posible.`,summary,category,keywords,about:[summary,"La herramienta funciona directamente en el navegador para ofrecer resultados inmediatos."],steps:["Introduce los datos que quieras procesar.","Ejecuta la herramienta o espera al resultado automático.","Revisa y copia el resultado."],faq:[faq("¿Necesito registrarme?","No. La herramienta funciona sin cuenta."),faq("¿Se envían mis datos?","Las operaciones compatibles se realizan localmente en el navegador.")],kind,config});
