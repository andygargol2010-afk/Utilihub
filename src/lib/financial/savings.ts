import type { FinancialDefinition } from "./types";
import { money, pct } from "./types";
const f=(key:string,label:string,defaultValue:number,unit="")=>({key,label,defaultValue,unit});
const error=(value:string)=>[{label:"Error",value}];
export const SAVINGS_TOOLS:FinancialDefinition[]=[
{slug:"ahorro-periodico",fields:[f("current","Ahorro actual",1000,"$"),f("monthly","Aporte mensual",300,"$"),f("rate","Rentabilidad anual",4,"%"),f("years","Años",5)],calculate:v=>{if(v.current<0||v.monthly<0||v.years<0||v.rate<=-100)return error("Ahorro, aporte y años no pueden ser negativos; la rentabilidad debe ser mayor que -100 %.");const m=v.rate/100/12,n=Math.round(v.years*12),future=m===0?v.current+v.monthly*n:v.current*Math.pow(1+m,n)+v.monthly*((Math.pow(1+m,n)-1)/m);return[{label:"Capital futuro",value:money(future)},{label:"Total aportado",value:money(v.current+v.monthly*n)},{label:"Crecimiento",value:money(future-v.current-v.monthly*n)}]}},
{slug:"ahorro-para-una-compra",fields:[f("target","Objetivo",20000,"$"),f("current","Ahorro actual",5000,"$"),f("months","Meses disponibles",18)],calculate:v=>{if(v.target<0||v.current<0||v.months<=0)return error("El objetivo y el ahorro no pueden ser negativos y los meses deben ser mayores que 0.");return[{label:"Aporte mensual necesario",value:money(Math.max(0,v.target-v.current)/v.months)},{label:"Falta ahorrar",value:money(Math.max(0,v.target-v.current))}]}},
{slug:"fondo-de-emergencia",fields:[f("expenses","Gastos mensuales",1500,"$"),f("months","Meses de cobertura",6),f("current","Ahorros actuales",4000,"$")],calculate:v=>{if(v.expenses<0||v.months<0||v.current<0)return error("Los gastos, meses y ahorros no pueden ser negativos.");const target=v.expenses*v.months;return[{label:"Fondo objetivo",value:money(target)},{label:"Ahorro actual",value:money(v.current)},{label:"Faltante",value:money(Math.max(0,target-v.current))}]}},
{slug:"tasa-de-ahorro",fields:[f("income","Ingresos mensuales",3000,"$"),f("saving","Ahorro mensual",900,"$")],calculate:v=>{if(v.income<=0)return error("Los ingresos deben ser mayores que 0.");return[{label:"Tasa de ahorro",value:pct(v.saving/v.income*100)},{label:"Gasto mensual",value:money(v.income-v.saving)}]}},
{slug:"meta-de-ahorro-con-rendimiento",fields:[f("target","Objetivo",50000,"$"),f("current","Capital actual",5000,"$"),f("rate","Rentabilidad anual",5,"%"),f("months","Plazo",36)],calculate:v=>{if(v.target<0||v.current<0||v.months<=0||v.rate<=-100)return error("Objetivo y capital no pueden ser negativos, el plazo debe ser > 0 y la rentabilidad debe ser mayor que -100 %.");const r=v.rate/100/12,n=Math.round(v.months);let monthly;if(r===0)monthly=(v.target-v.current)/n;else{const f=Math.pow(1+r,n);monthly=(v.target-v.current*f)*r/(f-1)}return[{label:"Aporte mensual necesario",value:money(Math.max(0,monthly))},{label:"Aporte total",value:money(Math.max(0,monthly)*n)}]}}
];
