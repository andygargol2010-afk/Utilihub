import{useState}from"react";import{Link}from"@tanstack/react-router";import{Menu,X}from"lucide-react";import{CATEGORIES}from"@/lib/tools";
export function SiteHeader(){const[open,setOpen]=useState(false);return <header className="sticky top-0 z-40 border-b border-border bg-background/90 backdrop-blur"><div className="container-page flex h-16 items-center justify-between gap-4"><Link to="/" className="flex items-center gap-2 font-display text-lg font-bold tracking-tight"><span className="grid size-8 place-items-center rounded-lg bg-primary font-mono text-primary-foreground">U</span>UtiliHub</Link><nav aria-label="Principal" className="hidden items-center gap-1 lg:flex"><Link to="/herramientas" className="rounded-md px-3 py-2 text-sm font-medium text-muted-foreground hover:bg-accent hover:text-accent-foreground">Herramientas</Link>{CATEGORIES.map(c=><Link key={c.slug} to="/categoria/$slug" params={{slug:c.slug}} className="rounded-md px-3 py-2 text-sm font-medium text-muted-foreground hover:bg-accent hover:text-accent-foreground">{c.name}</Link>)}</nav><button type="button" className="rounded-md p-2 lg:hidden" aria-expanded={open} aria-label={open?"Cerrar menú":"Abrir menú"} onClick={()=>setOpen(v=>!v)}>{open?<X className="size-5"/>:<Menu className="size-5"/>}</button></div>{open&&<nav aria-label="Móvil" className="border-t border-border lg:hidden"><div className="container-page grid gap-1 py-3"><Link to="/herramientas" onClick={()=>setOpen(false)} className="rounded-md px-3 py-2 text-sm font-medium hover:bg-accent">Todas las herramientas</Link>{CATEGORIES.map(c=><Link key={c.slug} to="/categoria/$slug" params={{slug:c.slug}} onClick={()=>setOpen(false)} className="rounded-md px-3 py-2 text-sm font-medium hover:bg-accent">{c.name}</Link>)}</div></nav>}</header>;}