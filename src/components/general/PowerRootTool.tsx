import {useState} from "react";
import type {GeneralTool} from "@/lib/general/types";
export function PowerRootTool({tool:_tool,locale="en"}:{tool:GeneralTool;locale?:"en"|"es"}){const[base,setBase]=useState("");const[exponent,setExponent]=useState("");const[out,setOut]=useState("");const es=locale==="es";const run=()=>{const a=Number(base.replace(",",".")),b=Number(exponent.replace(",","."));try{if(!Number.isFinite(a)||!Number.isFinite(b))throw Error(es?"Introduce una base y un exponente válidos.":"Enter valid base and exponent.");const power=a**b;const root=a>=0?Math.sqrt(a):(es?"no es real":"not real");if(!Number.isFinite(power)&&typeof power!=="string")throw Error(es?"La potencia no produce un resultado real finito.":"Power does not produce a finite real result.");setOut(`${es?"Potencia":"Power"}: ${typeof power==="number"?power.toFixed(6):power} · ${es?"Raíz cuadrada":"Square root"}: ${typeof root==="number"?root.toFixed(6):root}`)}catch(e){setOut(e instanceof Error?e.message:(es?"Entrada no válida.":"Invalid input."))}};return <div className="space-y-4"><div className="grid gap-4 sm:grid-cols-2"><label className="space-y-1"><span className="text-sm font-medium">{es?"Base":"Base"}</span><input type="number" value={base} onChange={e=>setBase(e.target.value)} className="h-11 w-full rounded-xl border bg-background px-3"/></label><label className="space-y-1"><span className="text-sm font-medium">{es?"Exponente":"Exponent"}</span><input type="number" value={exponent} onChange={e=>setExponent(e.target.value)} className="h-11 w-full rounded-xl border bg-background px-3"/></label></div><button onClick={run} className="rounded-xl bg-primary px-4 py-2 font-bold text-primary-foreground">{es?"Calcular":"Calculate"}</button>{out&&<output className="block rounded-xl border bg-muted/30 p-4">{out}</output>}</div>}
