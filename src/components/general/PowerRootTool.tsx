import {useState} from "react";
import type {GeneralTool} from "@/lib/general/types";
export function PowerRootTool({tool}:{tool:GeneralTool}){const[base,setBase]=useState("");const[exponent,setExponent]=useState("");const[out,setOut]=useState("");const run=()=>{const a=Number(base.replace(",",".")),b=Number(exponent.replace(",","."));try{if(!Number.isFinite(a)||!Number.isFinite(b))throw Error("Enter valid base and exponent.");const power=a**b;const root=a>=0?Math.sqrt(a):"not real";if(!Number.isFinite(power)&&typeof power!=="string")throw Error("Power does not produce a finite real result.");setOut(`Power: ${typeof power==="number"?power.toFixed(6):power} · Square root: ${typeof root==="number"?root.toFixed(6):root}`)}catch(e){setOut(e instanceof Error?e.message:"Invalid input.")}};return <div className="space-y-4"><div className="grid gap-4 sm:grid-cols-2"><label className="space-y-1"><span className="text-sm font-medium">Base</span><input type="number" value={base} onChange={e=>setBase(e.target.value)} className="h-11 w-full rounded-xl border bg-background px-3"/></label><label className="space-y-1"><span className="text-sm font-medium">Exponent</span><input type="number" value={exponent} onChange={e=>setExponent(e.target.value)} className="h-11 w-full rounded-xl border bg-background px-3"/></label></div><button onClick={run} className="rounded-xl bg-primary px-4 py-2 font-bold text-primary-foreground">Calculate</button>{out&&<output className="block rounded-xl border bg-muted/30 p-4">{out}</output>}</div>}
