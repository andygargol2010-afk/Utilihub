import {useState} from "react";
import type {GeneralTool} from "@/lib/general/types";
export function SequenceTool({tool}:{tool:GeneralTool}){const[first,setFirst]=useState("");const[step,setStep]=useState("");const[count,setCount]=useState("");const[type,setType]=useState<"arithmetic"|"geometric">("arithmetic");const[out,setOut]=useState("");const run=()=>{const a=Number(first.replace(",",".")),r=Number(step.replace(",",".")),n=Number(count);try{if(!Number.isFinite(a)||!Number.isFinite(r)||!Number.isInteger(n)||n<1||n>1000)throw Error("Introduce primer término, diferencia/razón válidos y entre 1 y 1000 términos.");const terms=Array.from({length:n},(_,i)=>type==="arithmetic"?a+r*i:a*Math.pow(r,i));setOut(`Secuencia ${type==="arithmetic"?"aritmética":"geométrica"}: ${terms.join(", ")}`)}catch(e){setOut(e instanceof Error?e.message:"Entrada inválida.")}};return <div className="space-y-4"><div className="grid gap-4 sm:grid-cols-2"><label className="space-y-1"><span className="text-sm font-medium">Primer término</span><input type="number" value={first} onChange={e=>setFirst(e.target.value)} className="h-11 w-full rounded-xl border bg-background px-3"/></label><label className="space-y-1"><span className="text-sm font-medium">{type==="arithmetic"?"Diferencia":"Razón"}</span><input type="number" value={step} onChange={e=>setStep(e.target.value)} className="h-11 w-full rounded-xl border bg-background px-3"/></label><label className="space-y-1"><span className="text-sm font-medium">Número de términos</span><input type="number" min="1" max="1000" value={count} onChange={e=>setCount(e.target.value)} className="h-11 w-full rounded-xl border bg-background px-3"/></label><label className="space-y-1"><span className="text-sm font-medium">Tipo de secuencia</span><select value={type} onChange={e=>setType(e.target.value as "arithmetic"|"geometric")} className="h-11 w-full rounded-xl border bg-background px-3"><option value="arithmetic">Aritmética</option><option value="geometric">Geométrica</option></select></label></div><button onClick={run} className="rounded-xl bg-primary px-4 py-2 font-bold text-primary-foreground">Generar</button>{out&&<output className="block break-words rounded-xl border bg-muted/30 p-4">{out}</output>}</div>}
