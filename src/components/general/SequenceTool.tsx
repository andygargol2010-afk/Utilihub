import {useState} from "react";
import type {GeneralTool} from "@/lib/general/types";
export function SequenceTool({tool:_tool,locale="en"}:{tool:GeneralTool;locale?:"en"|"es"}){const[first,setFirst]=useState("");const[step,setStep]=useState("");const[count,setCount]=useState("");const[type,setType]=useState<"arithmetic"|"geometric">("arithmetic");const[out,setOut]=useState("");const es=locale==="es";const run=()=>{const a=Number(first.replace(",",".")),r=Number(step.replace(",",".")),n=Number(count);try{if(!Number.isFinite(a)||!Number.isFinite(r)||!Number.isInteger(n)||n<1||n>1000)throw Error(es?"Introduce un primer término, diferencia/razón válidos y entre 1 y 1000 términos.":"Enter valid first term, difference/ratio, and between 1 and 1000 terms.");const terms=Array.from({length:n},(_,i)=>type==="arithmetic"?a+r*i:a*Math.pow(r,i));setOut(`${es?"Sucesión":"Sequence"} ${type==="arithmetic"?(es?"aritmética":"arithmetic"):(es?"geométrica":"geometric")}: ${terms.join(", ")}`)}catch(e){setOut(e instanceof Error?e.message:(es?"Entrada no válida.":"Invalid input."))}};return <div className="space-y-4"><div className="grid gap-4 sm:grid-cols-2"><label className="space-y-1"><span className="text-sm font-medium">{es?"Primer término":"First term"}</span><input type="number" value={first} onChange={e=>setFirst(e.target.value)} className="h-11 w-full rounded-xl border bg-background px-3"/></label><label className="space-y-1"><span className="text-sm font-medium">{type==="arithmetic"?(es?"Diferencia":"Difference"):(es?"Razón":"Ratio")}</span><input type="number" value={step} onChange={e=>setStep(e.target.value)} className="h-11 w-full rounded-xl border bg-background px-3"/></label><label className="space-y-1"><span className="text-sm font-medium">{es?"Número de términos":"Number of terms"}</span><input type="number" min="1" max="1000" value={count} onChange={e=>setCount(e.target.value)} className="h-11 w-full rounded-xl border bg-background px-3"/></label><label className="space-y-1"><span className="text-sm font-medium">{es?"Tipo de sucesión":"Sequence type"}</span><select value={type} onChange={e=>setType(e.target.value as "arithmetic"|"geometric")} className="h-11 w-full rounded-xl border bg-background px-3"><option value="arithmetic">{es?"Aritmética":"Arithmetic"}</option><option value="geometric">{es?"Geométrica":"Geometric"}</option></select></label></div><button onClick={run} className="rounded-xl bg-primary px-4 py-2 font-bold text-primary-foreground">{es?"Generar":"Generate"}</button>{out&&<output className="block break-words rounded-xl border bg-muted/30 p-4">{out}</output>}</div>}
