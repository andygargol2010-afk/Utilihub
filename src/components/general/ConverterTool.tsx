import { useState } from "react";
import type { GeneralTool } from "@/lib/general/types";
const units: Record<string, Record<string, number>> = {
 area:{"m²":1,"km²":1e6,"ft²":0.092903,acres:4046.8564224,ha:10000},volume:{L:1,mL:0.001,gal:3.785411784,"m³":1000},speed:{"km/h":1,mph:1.609344,"m/s":3.6,kn:1.852},energy:{J:1,cal:4.184,kWh:3600000,BTU:1055.05585262},power:{W:1,kW:1000,hp:745.699872},pressure:{Pa:1,bar:100000,atm:101325,psi:6894.757293},frequency:{Hz:1,kHz:1000,MHz:1e6,GHz:1e9},data:{bit:1,byte:8,KB:8000,MB:8e6,GB:8e9,TB:8e12},mass:{mg:1,g:1000,kg:1e6,t:1e9,oz:28349.523125,lb:453592.37},length:{mm:0.001,cm:0.01,m:1,km:1000,in:0.0254,ft:0.3048,mi:1609.344},time:{s:1,min:60,h:3600,d:86400,w:604800},
};
const sets: Record<string,string[]>={area:["m²","km²","ft²","acres","ha"],volume:["L","mL","gal","m³"],speed:["km/h","mph","m/s","kn"],energy:["J","cal","kWh","BTU"],power:["W","kW","hp"],pressure:["Pa","bar","atm","psi"],frequency:["Hz","kHz","MHz","GHz"],data:["bit","byte","KB","MB","GB","TB"],mass:["mg","g","kg","t","oz","lb"],length:["mm","cm","m","km","in","ft","mi"],time:["s","min","h","d","w"]};
const tempToC=(v:number,u:string)=>u==="°C"?v:u==="°F"?(v-32)*5/9:v-273.15;const cToTemp=(v:number,u:string)=>u==="°C"?v:u==="°F"?v*9/5+32:v+273.15;
const hexToRgb=(value:string):{r:number;g:number;b:number}|null=>{const hex=value.replace(/^#/,'').trim();const expanded=hex.length===3?[...hex].map(x=>x+x).join(''):hex;if(!/^[0-9a-f]{6}$/i.test(expanded))return null;return{r:parseInt(expanded.slice(0,2),16),g:parseInt(expanded.slice(2,4),16),b:parseInt(expanded.slice(4,6),16)}};
const parseRgb=(value:string):[number,number,number]=>{const p=value.split(/[,;\s]+/).filter(Boolean).map(Number);if(p.length!==3||p.some(x=>!Number.isInteger(x)||x<0||x>255))throw new Error("Introduce R, G y B como tres enteros entre 0 y 255.");return[p[0],p[1],p[2]]};
const rgbToHsl=(r:number,g:number,b:number):string=>{const[rr,gg,bb]=[r,g,b].map(x=>x/255),max=Math.max(rr,gg,bb),min=Math.min(rr,gg,bb),l=(max+min)/2;if(max===min)return`HSL: 0°, 0%, ${(l*100).toFixed(2)}%`;const d=max-min,s=l>0.5?d/(2-max-min):d/(max+min);let h=max===rr?(gg-bb)/d+(gg<bb?6:0):max===gg?(bb-rr)/d+2:(rr-gg)/d+4;h/=6;return`HSL: ${(h*360).toFixed(2)}°, ${(s*100).toFixed(2)}%, ${(l*100).toFixed(2)}%`};
export function ConverterTool({tool}:{tool:GeneralTool}){const[value,setValue]=useState("");const[from,setFrom]=useState("");const[to,setTo]=useState("");const[angleDirection,setAngleDirection]=useState<"degrees-to-radians"|"radians-to-degrees">("degrees-to-radians");const[out,setOut]=useState("");const base=tool.slug.replace("conversor-","");const kind=base==="datos"?"data":base==="velocidad"?"speed":base==="energia"?"energy":base==="potencia"?"power":base==="presion"?"pressure":base==="frecuencia"?"frequency":base==="area"?"area":base==="volumen"?"volume":base==="masa"?"mass":base==="longitud"?"length":base==="tiempo"?"time":"";const options=sets[kind]??[];const convert=()=>{try{const raw=value.trim();if(!raw)throw new Error("Introduce un valor para convertir.");if(tool.slug==="hexadecimal-decimal"){if(!/^(?:0x)?[0-9a-f]+$/i.test(raw))throw new Error("Introduce un número hexadecimal válido.");setOut(String(parseInt(raw.replace(/^0x/i,""),16)));return}if(tool.slug==="hex-rgb"){const rgb=hexToRgb(raw);if(!rgb)throw new Error("Introduce un HEX válido de 3 o 6 dígitos.");setOut(`RGB: ${rgb.r}, ${rgb.g}, ${rgb.b}`);return}if(tool.slug==="rgb-hsl"){setOut(rgbToHsl(...parseRgb(raw)));return}if(tool.slug==="rgb-hex"){const[r,g,b]=parseRgb(raw);setOut(`#${[r,g,b].map(x=>x.toString(16).padStart(2,"0")).join("").toUpperCase()}`);return}if(tool.slug==="binario-decimal"){if(!/^(?:0b)?[01]+$/i.test(raw))throw new Error("Introduce un número binario válido.");setOut(String(parseInt(raw.replace(/^0b/i,""),2)));return}if(tool.slug==="decimal-binario"||tool.slug==="decimal-hexadecimal"){const number=Number(raw);if(!Number.isSafeInteger(number)||number<0)throw new Error("Introduce un entero decimal no negativo dentro del rango seguro de JavaScript.");setOut(tool.slug==="decimal-binario"?number.toString(2):number.toString(16).toUpperCase());return}if(tool.slug==="grados-radianes"||tool.slug==="conversor-angulos"){const number=Number(raw);if(!Number.isFinite(number))throw new Error("Introduce un ángulo numérico válido.");setOut(tool.slug==="grados-radianes"||angleDirection==="degrees-to-radians"?`${number}° = ${(number*Math.PI/180).toFixed(10)} rad`:`${number} rad = ${(number*180/Math.PI).toFixed(10)}°`);return}if(tool.slug==="conversor-temperatura"){const number=Number(raw);const source=from||"°C",target=to||"°F";if(!["°C","°F","K"].includes(source)||!["°C","°F","K"].includes(target))throw new Error("Selecciona escalas de temperatura válidas.");if(!Number.isFinite(number)||source==="K"&&number<0)throw new Error("Introduce una temperatura válida.");setOut(`${number} ${source} = ${cToTemp(tempToC(number,source),target).toFixed(6)} ${target}`);return}const number=Number(raw.replace(",","."));if(!Number.isFinite(number))throw new Error("Introduce un número válido.");if(kind){const source=from||options[0],target=to||options[1]||options[0],sf=units[kind]?.[source],tf=units[kind]?.[target];if(!source||!target||sf===undefined||tf===undefined)throw new Error("Selecciona unidades de origen y destino válidas.");setOut(`${number} ${source} = ${(number*sf/tf).toPrecision(10)} ${target}`);return}throw new Error(`Conversor sin implementación específica: ${tool.slug}`)}catch(error){setOut(error instanceof Error?error.message:"No se pudo convertir el valor.")}};return <div className="space-y-4"><label className="block space-y-1"><span className="text-sm font-medium">Valor</span><input value={value} onChange={e=>setValue(e.target.value)} className="h-11 w-full rounded-xl border bg-background px-3" placeholder={tool.slug.includes("rgb")?"255, 99, 71":"100"} inputMode="decimal"/></label>{tool.slug==="conversor-angulos"&&<label className="block space-y-1 text-sm font-medium">Dirección<select value={angleDirection} onChange={e=>setAngleDirection(e.target.value as "degrees-to-radians"|"radians-to-degrees")} className="mt-1 h-11 w-full rounded-xl border bg-background px-3"><option value="degrees-to-radians">Grados → radianes</option><option value="radians-to-degrees">Radianes → grados</option></select></label>}{(options.length>1||tool.slug==="conversor-temperatura")&&<div className="grid gap-4 sm:grid-cols-2"><label className="space-y-1 text-sm font-medium">Desde<select value={from} onChange={e=>setFrom(e.target.value)} className="mt-1 h-11 w-full rounded-xl border bg-background px-3"><option value="">Predeterminado</option>{(options.length?options:["°C","°F","K"]).map(option=><option key={option}>{option}</option>)}</select></label><label className="space-y-1 text-sm font-medium">Hacia<select value={to} onChange={e=>setTo(e.target.value)} className="mt-1 h-11 w-full rounded-xl border bg-background px-3"><option value="">Predeterminado</option>{(options.length?options:["°C","°F","K"]).map(option=><option key={option}>{option}</option>)}</select></label></div>}<button className="rounded-xl bg-primary px-4 py-2 font-bold text-primary-foreground" onClick={convert}>Convertir</button>{out&&<output aria-live="polite" className="block break-all rounded-xl border bg-muted/30 p-4">{out}</output>}</div>}