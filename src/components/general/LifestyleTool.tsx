import {useState}from"react";
import type {GeneralTool}from"@/lib/general/types";
export function LifestyleTool({tool}:{tool:GeneralTool}){const[value,setValue]=useState("100"),[a,setA]=useState("0"),[b,setB]=useState("0"),[out,setOut]=useState("");const run=()=>{const n=Number(value.replace(",",".")),x=Number(a.replace(",",".")),y=Number(b.replace(",","."));let result="";if(!Number.isFinite(n)||!Number.isFinite(x)||!Number.isFinite(y)){setOut("Introduce valores válidos.");return}switch(tool.slug){case"gramos-tazas":result=`Aproximación para harina: ${(n/125).toFixed(2)} tazas.`;break;case"ml-cucharadas":result=`${n} ml ≈ ${(n/15).toFixed(2)} cucharadas ≈ ${(n/5).toFixed(2)} cucharaditas.`;break;case"zona-horaria-viaje":{const local=x+y;const hours=((local%24)+24)%24;result=`Hora convertida: ${Math.floor(hours).toString().padStart(2,"0")}:${Math.round((hours%1)*60).toString().padStart(2,"0")}`;break}case"duracion-vuelo":{let duration=y-x;if(duration<0)duration+=24;result=`Duración aproximada: ${Math.floor(duration)} h ${Math.round((duration%1)*60)} min`;break}default:result="Herramienta de estilo de vida no implementada."}setOut(result)};return <div className="space-y-4"><label className="block space-y-1"><span className="text-sm font-medium">Cantidad / hora</span><input type={tool.slug.includes("zona-horaria")||tool.slug.includes("vuelo")?"time":"number"} value={value} onChange={e=>setValue(e.target.value)} className="h-11 w-full rounded-xl border bg-background px-3"/></label>{(tool.slug.includes("zona-horaria")||tool.slug.includes("vuelo"))&&<div className="grid gap-4 sm:grid-cols-2"><label className="space-y-1"><span className="text-sm font-medium">Desfase / salida</span><input type="number" value={a} onChange={e=>setA(e.target.value)} className="h-11 w-full rounded-xl border bg-background px-3"/></label><label className="space-y-1"><span className="text-sm font-medium">Destino / llegada</span><input type="number" value={b} onChange={e=>setB(e.target.value)} className="h-11 w-full rounded-xl border bg-background px-3"/></label></div>}<button type="button" onClick={run} className="rounded-xl bg-primary px-4 py-2 font-bold text-primary-foreground">Calcular</button>{out&&<output className="block rounded-xl border bg-muted/30 p-4">{out}</output>}</div>}
