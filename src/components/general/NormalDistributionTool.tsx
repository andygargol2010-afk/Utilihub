import {useState} from "react";
import type {GeneralTool} from "@/lib/general/types";
const erf=(x:number)=>{const sign=x<0?-1:1;const a=Math.abs(x),t=1/(1+0.3275911*a);const y=1-((((1.061405429*t-1.453152027)*t+1.421413741)*t-0.284496736)*t+0.254829592)*t*Math.exp(-a*a);return sign*y};
export function NormalDistributionTool({tool:_tool}:{tool:GeneralTool}){const[x,setX]=useState(""),[mean,setMean]=useState(""),[sd,setSd]=useState(""),[out,setOut]=useState("");const run=()=>{const a=Number(x),m=Number(mean),s=Number(sd);try{if(![a,m,s].every(Number.isFinite)||s<=0)throw Error("Introduce x, media y una desviación estándar mayor que 0.");setOut(`P(X ≤ x): ${(.5*(1+erf((a-m)/(s*Math.SQRT2)))).toFixed(6)}`)}catch(e){setOut(e instanceof Error?e.message:"Entrada inválida.")}};return <div className="space-y-4"><div className="grid gap-4 sm:grid-cols-3">{[["x",x,setX],["Media",mean,setMean],["Desviación estándar",sd,setSd]].map(([label,value,setter])=><label key={String(label)} className="space-y-1"><span className="text-sm font-medium">{label}</span><input type="number" value={String(value)} onChange={e=>(setter as (v:string)=>void)(e.target.value)} className="h-11 w-full rounded-xl border bg-background px-3"/></label>)}</div><button onClick={run} className="rounded-xl bg-primary px-4 py-2 font-bold text-primary-foreground">Calcular</button>{out&&<output className="block rounded-xl border bg-muted/30 p-4">{out}</output>}</div>}
