import {useMemo,useState} from "react";
import type {GeneralTool as Tool} from "@/lib/general/types";
const nums=(s:string)=>s.split(/[\n,;]+/).map(x=>Number(x.trim().replace(",","."))).filter(Number.isFinite);
const num=(s:string)=>Number(s.trim().replace(",","."));
function stats(slug:string,s:string){const xs=nums(s).sort((a,b)=>a-b);if(!xs.length)return"Enter at least one number.";const mean=xs.reduce((a,b)=>a+b,0)/xs.length;const mid=xs.length%2?xs[(xs.length-1)/2]:(xs[xs.length/2-1]+xs[xs.length/2])/2;const variance=xs.reduce((a,b)=>a+(b-mean)**2,0)/xs.length;if(slug==="promedio")return`Average: ${mean}`;if(slug==="mediana")return`Median: ${mid}`;if(slug==="rango")return`Range: ${xs.at(-1)!-xs[0]}`;if(slug==="varianza")return`Variance: ${variance}`;if(slug==="desviacion-estandar")return`Standard deviation: ${Math.sqrt(variance)}`;return`Count: ${xs.length} · Average: ${mean}`}
function calculate(slug:string,a:string,b:string){const A=num(a),B=num(b);if(!Number.isFinite(A)||!Number.isFinite(B))return"Enter valid numbers.";if(slug.includes("mcd")||slug.includes("mcm")){let x=Math.abs(Math.trunc(A)),y=Math.abs(Math.trunc(B));while(y){const t=x%y;x=y;y=t}return slug.includes("mcd")?`GCD: ${x}`:`LCM: ${Math.abs(Math.trunc(A)*Math.trunc(B))/(x||1)}`};if(slug.includes("factorial")){const n=Math.trunc(A);if(n<0||n>170)return"Enter an integer between 0 and 170.";let r=1;for(let i=2;i<=n;i++)r*=i;return`Factorial: ${r}`};if(slug.includes("potencias"))return`Power: ${A**B}`;if(slug.includes("porcentaje"))return`Result: ${(A*B)/100}`;if(slug==="grados-radianes")return`Radians: ${A*Math.PI/180}`;if(slug==="densidad")return`Density: ${A/B}`;if(slug==="velocidad-fisica")return`Speed: ${A/B}`;if(slug==="aceleracion")return`Acceleration: ${A/B}`;if(slug==="fuerza")return`Force: ${A*B} N`;if(slug==="energia-cinetica")return`Kinetic energy: ${0.5*A*B*B} J`;if(slug==="ley-de-ohm")return`Voltage: ${A*B} V`;if(slug==="potencia-electrica")return`Power: ${A*B} W`;return"This tool needs a specific formula."}
export function GeneralTool({tool}:{tool:Tool}){const[a,setA]=useState("");const[b,setB]=useState("");const[out,setOut]=useState("");const[text,setText]=useState("");const[copied,setCopied]=useState(false);const process=()=>{let x=text;if(tool.slug==="mayusculas")x=x.toUpperCase();else if(tool.slug==="minusculas")x=x.toLowerCase();else if(tool.slug==="invertir-texto")x=[...x].reverse().join("");else if(tool.slug==="quitar-espacios"||tool.slug==="limpiar-texto")x=x.replace(/\s+/g," ").trim();else if(tool.slug==="lineas-unicas")x=[...new Set(x.split(/\r?\n/))].join("\n");else if(tool.slug==="ordenar-lineas")x=x.split(/\r?\n/).sort((u,v)=>u.localeCompare(v)).join("\n");else if(tool.slug==="slug-generator")x=x.toLowerCase().normalize("NFD").replace(/[\u0300-\u036f]/g,"").replace(/[^a-z0-9]+/g,"-").replace(/^-|-$/g,"");setText(x)};const count=useMemo(()=>({chars:text.length,words:text.trim()?text.trim().split(/\s+/).length:0,lines:text?text.split(/\r?\n/).length:0}),[text]);const copy=async()=>{await navigator.clipboard.writeText(text);setCopied(true);setTimeout(()=>setCopied(false),1000)};if(tool.kind==="stats")return <div className="space-y-4"><label className="block text-sm font-medium">Numbers to analyze</label><textarea value={a} onChange={e=>setA(e.target.value)} className="min-h-32 w-full rounded-xl border bg-background p-4" placeholder="e.g. 5, 10, 15, 20"/><p className="text-sm text-muted-foreground">Separate values with commas, semicolons, or line breaks.</p><button className="rounded-xl bg-primary px-4 py-2 font-bold text-primary-foreground" onClick={()=>setOut(stats(tool.slug,a))}>Calculate</button>{out&&<output className="block rounded-xl border bg-muted/30 p-4">{out}</output>}</div>;if(["text","code","encode"].includes(tool.kind))return <div className="space-y-4"><label className="block text-sm font-medium">Input text</label><textarea value={text} onChange={e=>setText(e.target.value)} className="min-h-48 w-full rounded-xl border bg-background p-4" placeholder="Type or paste here…"/><div className="flex flex-wrap gap-2"><button className="rounded-xl bg-primary px-4 py-2 text-sm font-bold text-primary-foreground" onClick={process}>Process</button><button className="rounded-xl border px-4 py-2 text-sm" onClick={copy}>{copied?"Copied":"Copy"}</button></div><p className="text-sm text-muted-foreground">{count.words} words · {count.chars} characters · {count.lines} lines</p></div>;if(tool.kind==="generator")return <div className="space-y-4"><button className="rounded-xl bg-primary px-4 py-2 font-bold text-primary-foreground" onClick={()=>setOut(tool.slug.includes("uuid")?crypto.randomUUID():Math.random().toString(36).slice(2,12))}>Generate</button>{out&&<output className="block break-all rounded-xl border bg-muted/30 p-4">{out}</output>}</div>;return <div className="space-y-4"><div className="grid gap-4 sm:grid-cols-2"><label className="space-y-1"><span className="text-sm font-medium">First value</span><input value={a} onChange={e=>setA(e.target.value)} placeholder="e.g. 10" className="h-11 w-full rounded-xl border bg-background px-3"/></label><label className="space-y-1"><span className="text-sm font-medium">Second value</span><input value={b} onChange={e=>setB(e.target.value)} placeholder="e.g. 5" className="h-11 w-full rounded-xl border bg-background px-3"/></label></div><button className="rounded-xl bg-primary px-4 py-2 font-bold text-primary-foreground" onClick={()=>setOut(calculate(tool.slug,a,b))}>Calculate</button>{out&&<output className="block rounded-xl border bg-muted/30 p-4">{out}</output>}</div>}
