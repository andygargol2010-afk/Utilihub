import {useState} from "react";
import type {GeneralTool} from "@/lib/general/types";
import {generateBySlug} from "@/lib/general/generator-engine";

const randomInt=(min:number,max:number)=>Math.floor(Math.random()*(max-min+1))+min;
const shuffle=(items:string[])=>{const a=[...items];for(let i=a.length-1;i>0;i--){const j=randomInt(0,i);[a[i],a[j]]=[a[j],a[i]]}return a};
export function GeneratorTool({tool}:{tool:GeneralTool}){const[out,setOut]=useState("");const[min,setMin]=useState("1");const[max,setMax]=useState("100");const[sides,setSides]=useState("6");const[count,setCount]=useState("1");const[list,setList]=useState("");const run=()=>{try{if(tool.slug==="generador-numeros"){const a=Number(min),b=Number(max);if(!Number.isInteger(a)||!Number.isInteger(b)||a>b)throw new Error("Introduce un rango entero válido.");setOut(String(randomInt(a,b)));return}if(tool.slug==="generador-dados"){const s=Number(sides),n=Number(count);if(!Number.isInteger(s)||s<2||s>10000||!Number.isInteger(n)||n<1||n>1000)throw new Error("Los dados requieren caras entre 2 y 10000 y una cantidad entre 1 y 1000.");setOut(Array.from({length:n},()=>String(randomInt(1,s))).join(" · "));return}if(tool.slug==="sorteo"){const items=list.split(/\r?\n|,/).map(x=>x.trim()).filter(Boolean);if(!items.length)throw new Error("Introduce al menos un elemento para sortear.");setOut(items[randomInt(0,items.length-1)]);return}if(tool.slug==="lista-aleatoria"){const items=list.split(/\r?\n|,/).map(x=>x.trim()).filter(Boolean);if(!items.length)throw new Error("Introduce al menos un elemento para mezclar.");setOut(shuffle(items).join("\n"));return}setOut(generateBySlug(tool.slug))}catch(error){setOut(error instanceof Error?error.message:"No se pudo generar el resultado.")}};const needsList=tool.slug==="sorteo"||tool.slug==="lista-aleatoria";return <div className="space-y-4">{tool.slug==="generador-numeros"&&<div className="grid gap-4 sm:grid-cols-2"><label className="space-y-1 text-sm font-medium">Mínimo<input type="number" value={min} onChange={e=>setMin(e.target.value)} className="mt-1 h-11 w-full rounded-xl border bg-background px-3"/></label><label className="space-y-1 text-sm font-medium">Máximo<input type="number" value={max} onChange={e=>setMax(e.target.value)} className="mt-1 h-11 w-full rounded-xl border bg-background px-3"/></label></div>}{tool.slug==="generador-dados"&&<div className="grid gap-4 sm:grid-cols-2"><label className="space-y-1 text-sm font-medium">Caras por dado<input type="number" min="2" value={sides} onChange={e=>setSides(e.target.value)} className="mt-1 h-11 w-full rounded-xl border bg-background px-3"/></label><label className="space-y-1 text-sm font-medium">Cantidad de dados<input type="number" min="1" value={count} onChange={e=>setCount(e.target.value)} className="mt-1 h-11 w-full rounded-xl border bg-background px-3"/></label></div>}{needsList&&<label className="block text-sm font-medium">Elementos<textarea value={list} onChange={e=>setList(e.target.value)} placeholder="Un elemento por línea o separados por comas" className="mt-1 min-h-32 w-full rounded-xl border bg-background p-4"/></label>}<button onClick={run} className="rounded-xl bg-primary px-4 py-2 font-bold text-primary-foreground">Generar</button>{out&&<output className="block whitespace-pre-wrap break-all rounded-xl border bg-muted/30 p-4">{out}</output>}</div>}
