import {useState} from "react";
import type {GeneralTool} from "@/lib/general/types";
const factorial=(n:number)=>{let r=1;for(let i=2;i<=n;i++)r*=i;return r};
export function PoissonTool({tool:_tool}:{tool:GeneralTool}){const[k,setK]=useState(""),[lambda,setLambda]=useState(""),[out,setOut]=useState("");const run=()=>{const n=Number(k),l=Number(lambda);try{if(!Number.isInteger(n)||n<0||!Number.isFinite(l)||l<=0)throw Error("k debe ser un entero ≥ 0 y λ debe ser mayor que 0.");const result=Math.exp(-l)*l**n/factorial(n);if(!Number.isFinite(result))throw Error("Los valores son demasiado grandes para este cálculo.");setOut(`P(X = k): ${result.toFixed(8)}`)}catch(e){setOut(e instanceof Error?e.message:"Entrada inválida.")}};return <div className="space-y-4"><div className="grid gap-4 sm:grid-cols-2"><label className="space-y-1"><span className="text-sm font-medium">k eventos</span><input type="number" min="0" step="1" value={k} onChange={e=>setK(e.target.value)} className="h-11 w-full rounded-xl border bg-background px-3"/></label><label className="space-y-1"><span className="text-sm font-medium">λ (tasa media)</span><input type="number" min="0" step="any" value={lambda} onChange={e=>setLambda(e.target.value)} className="h-11 w-full rounded-xl border bg-background px-3"/></label></div><button onClick={run} className="rounded-xl bg-primary px-4 py-2 font-bold text-primary-foreground">Calcular</button>{out&&<output className="block rounded-xl border bg-muted/30 p-4">{out}</output>}</div>}
