import {useState}from"react";
import type {GeneralTool}from"@/lib/general/types";
const toMinutes=(value:string)=>{const[h,m]=value.split(":").map(Number);return Number.isFinite(h)&&Number.isFinite(m)?h*60+m:NaN};
export function ProductivityTool({tool}:{tool:GeneralTool}){const[a,setA]=useState("09:00"),[b,setB]=useState("17:00"),[text,setText]=useState(""),[out,setOut]=useState("");const run=()=>{let result="";switch(tool.slug){case"calculadora-horas-trabajadas":{const start=toMinutes(a),end=toMinutes(b);if(!Number.isFinite(start)||!Number.isFinite(end)){result="Introduce horarios válidos.";break}let diff=end-start;if(diff<0)diff+=1440;result=`Tiempo trabajado: ${Math.floor(diff/60)} h ${diff%60} min`;break}case"calculadora-tiempo":{const x=toMinutes(a),y=toMinutes(b);if(!Number.isFinite(x)||!Number.isFinite(y)){result="Introduce horas válidas.";break}const total=x+y;result=`Suma: ${Math.floor(total/60)} h ${total%60} min`;break}case"matriz-prioridades":{const lines=text.split(/\r?\n/).map(x=>x.trim()).filter(Boolean);result=lines.map((line,i)=>`${i+1}. ${line}`).join("\n")||"Añade tareas, una por línea.";break}case"planificador-semanal":result=["Lunes","Martes","Miércoles","Jueves","Viernes","Sábado","Domingo"].map(day=>`${day}: ${text||"Sin tareas"}`).join("\n");break;case"checklist":result=text.split(/\r?\n/).map(x=>x.trim()).filter(Boolean).map(x=>`- [ ] ${x}`).join("\n");break;case"habitos":{const values=text.split(/[\s,;]+/).map(Number).filter(Number.isFinite);const completed=values.filter(v=>v>0).length;result=values.length?`Días registrados: ${values.length}\nDías cumplidos: ${completed}\nCumplimiento: ${(completed/values.length*100).toFixed(2)} %`:"Introduce 0/1 por día.";break}default:result="Herramienta de productividad no implementada."}setOut(result)};const textTool=["matriz-prioridades","planificador-semanal","checklist","habitos"].includes(tool.slug);return <div className="space-y-4">{textTool?<label className="block space-y-2"><span className="text-sm font-semibold">Datos</span><textarea value={text} onChange={e=>setText(e.target.value)} placeholder={tool.slug==="habitos"?"1, 1, 0, 1…":"Una tarea por línea"} className="min-h-32 w-full rounded-xl border bg-background p-4"/></label>:<div className="grid gap-4 sm:grid-cols-2"><label className="space-y-1"><span className="text-sm font-medium">Inicio / primer valor</span><input type="time" value={a} onChange={e=>setA(e.target.value)} className="h-11 w-full rounded-xl border bg-background px-3"/></label><label className="space-y-1"><span className="text-sm font-medium">Fin / segundo valor</span><input type="time" value={b} onChange={e=>setB(e.target.value)} className="h-11 w-full rounded-xl border bg-background px-3"/></label></div>}<button type="button" onClick={run} className="rounded-xl bg-primary px-4 py-2 font-bold text-primary-foreground">Calcular</button>{out&&<output className="block whitespace-pre-wrap rounded-xl border bg-muted/30 p-4">{out}</output>}</div>}
