/**
 * Adsterra banner DISABLED — same network as Social Bar; mobile units were
 * triggering off-site redirects (frs2c.com / smartlink). No-op until a safe zone.
 */
export function AdsterraBanner(_props?: { label?: string }) {
  return null;
}
