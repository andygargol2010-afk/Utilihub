import{Link}from"@tanstack/react-router";import{ArrowUpRight,Star}from"lucide-react";import{ALL_CATEGORIES,type CatalogTool}from"@/lib/all-tools";
export function ToolCard({tool,isFavorite,onToggleFavorite}:{tool:CatalogTool;isFavorite?:boolean;onToggleFavorite?:(slug:string)=>void}){const category=ALL_CATEGORIES.find(c=>c.slug===tool.category)?.name;const href=tool.category==="finanzas"?`/finanzas/${tool.slug}`:"/herramientas/$slug";return <article className="surface-card group relative flex min-h-44 flex-col p-5 transition-all duration-200 hover:-translate-y-1 hover:border-primary/30 hover:shadow-lift"><div className="flex items-start justify-between gap-3"><span className="rounded-full bg-accent px-2.5 py-1 text-[11px] font-bold text-accent-foreground">{category}</span>{onToggleFavorite&&<button type="button" onClick={()=>onToggleFavorite(tool.slug)} aria-pressed={!!isFavorite} aria-label={isFavorite?`Quitar ${tool.name} de favoritos`:`Añadir ${tool.name} de favoritos`} className="relative z-10 rounded-lg border border-transparent p-1.5 text-muted-foreground hover:border-border hover:bg-accent hover:text-highlight"><Star className="size-4" fill={isFavorite?"currentColor":"none"}/></button>}</div><h3 className="mt-5 pr-5 text-lg font-bold"><Link to={href as any} params={tool.category==="finanzas"?undefined:{slug:tool.slug}} className="after:absolute after:inset-0 after:content-[''] group-hover:text-primary">{tool.name}</Link></h3><p className="mt-1.5 line-clamp-2 text-sm leading-5 text-muted-foreground">{tool.summary}</p><span className="mt-auto flex items-center gap-1 pt-5 text-xs font-bold text-primary">Abrir herramienta <ArrowUpRight className="size-3.5 transition-transform group-hover:translate-x-0.5 group-hover:-translate-y-0.5"/></span></article>;}
