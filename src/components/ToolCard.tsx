import{Link}from"@tanstack/react-router";import{Star}from"lucide-react";import{CATEGORIES,type Tool}from"@/lib/tools";
export function ToolCard({tool,isFavorite,onToggleFavorite}:{tool:Tool;isFavorite?:boolean;onToggleFavorite?:(slug:string)=>void}){const category=CATEGORIES.find(c=>c.slug===tool.category);return <article className="surface-card group relative flex flex-col p-5 transition-shadow hover:shadow-lift"><div className="flex items-start justify-between gap-3"><span className="rounded-full bg-accent px-2.5 py-1 text-xs font-medium text-accent-foreground">{category?.name}</span>{onToggleFavorite&&<button type="button" onClick={()=>onToggleFavorite(tool.slug)} aria-pressed={!!isFavorite} aria-label={isFavorite?`Quitar ${tool.name} de favoritos`:`Añadir ${tool.name} a favoritos`} className="relative z-10 rounded-md p-1 text-muted-foreground hover:text-highlight"><Star className="size-4" fill={isFavorite?"currentColor":"none"}/></button>}</div><h3 className="mt-3 text-lg font-semibold"><Link to="/herramientas/$slug" params={{slug:tool.slug}} className="after:absolute after:inset-0 after:content-[''] hover:text-primary">{tool.name}</Link></h3><p className="mt-1 text-sm text-muted-foreground">{tool.summary}</p></article>}