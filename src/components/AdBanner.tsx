/**
 * Display ads DISABLED — highrevenueformat invoke.js was force-redirecting
 * mobile users to external smartlinks (e.g. frs2c.com) without interaction.
 * Re-enable only with a verified non-redirecting unit.
 */
export function AdBanner() {
  return null;
}
