/**
 * DISABLED — highrevenueformat invoke.js redirects mobile users to smartlinks
 * (frs2c.com etc.) even with format:iframe. Re-enable only with a verified clean zone.
 */
export function AdBanner() {
  return null;
}
