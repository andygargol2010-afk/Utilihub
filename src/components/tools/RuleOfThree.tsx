import{useState}from"react";import{Input}from"@/components/ui/input";import{Label}from"@/components/ui/label";import{Button}from"@/components/ui/button";import{formatNumber,parseNumber}from"@/lib/units";
export default function RuleOfThree(){const[inverse,setInverse]=useState(false),[a,setA]=useState("3"),[b,setB]=useState("12"),[c,setC]=useState("7");const na=parseNumber(a),nb=parseNumber(b),nc=parseNumber(c);const x=[na,nb,nc].every(Number.isFinite)?inverse?(nc===0?NaN:na*nb/nc):(na===0?NaN:nb*nc/na):NaN;return <div className="space-y-6"><div className="flex gap-2"><Button type="button" variant={!inverse?"default":"outline"} onClick={()=>setInverse(false)}>Directa</Button><Button type="button" variant={inverse?"default":"outline"} onClick={()=>setInverse(true)}>Inversa</Button></div><div className="grid gap-4 sm:grid-cols-3"><div className="space-y-2"><Label htmlFor="r3-a">A</Label><Input id="r3-a" inputMode="decimal" value={a} onChange={e=>setA(e.target.value)}/></div><div className="space-y-2"><Label htmlFor="r3-b">B (corresponde a A)</Label><Input id="r3-b" inputMode="decimal" value={b} onChange={e=>setB(e.target.value)}/></div><div className="space-y-2"><Label htmlFor="r3-c">C</Label><Input id="r3-c" inputMode="decimal" value={c} onChange={e=>setC(e.target.value)}/></div></div><div className="rounded-xl bg-surface p-5"><p className="text-sm text-muted-foreground">Si {formatNumber(na)} → {formatNumber(nb)}, entonces {formatNumber(nc)} → X</p><p aria-live="polite" className="font-mono text-3xl font-semibold">X = {Number.isFinite(x)?formatNumber(x):"—"}</p><p className="mt-2 text-sm text-muted-foreground">Fórmula {inverse?"inversa":"directa"}: X = {inverse?"A × B ÷ C":"B × C ÷ A"}</p></div></div>}