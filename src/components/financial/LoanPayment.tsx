import{useState}from"react";import{FinancialField}from"./FinancialField";import{FinancialResult}from"./FinancialResult";import{formatNumber,parseNumber}from"@/lib/units";
export default function LoanPayment(){const[principal,setPrincipal]=useState("100000"),[rate,setRate]=useState("10"),[months,setMonths]=useState("60");const p=parseNumber(principal),r=parseNumber(rate)/100/12,n=Math.floor(parseNumber(months));if(![p,r,n].every(Number.isFinite)||p<=0||n<=0)return <div className="grid gap-4 sm:grid-cols-2"><FinancialField id="loan-p" label="Loan principal" value={principal} onChange={setPrincipal} suffix="$"/><FinancialField id="loan-rate" label="Annual rate" value={rate} onChange={setRate} suffix="%"/><FinancialField id="loan-months" label="Term" value={months} onChange={setMonths} suffix="months"/></div>;const payment=r===0?p/n:p*r*Math.pow(1+r,n)/(Math.pow(1+r,n)-1),total=payment*n;return <div className="space-y-6"><div className="grid gap-4 sm:grid-cols-2"><FinancialField id="loan-p" label="Loan principal" value={principal} onChange={setPrincipal} suffix="$"/><FinancialField id="loan-rate" label="Annual rate" value={rate} onChange={setRate} suffix="%"/><FinancialField id="loan-months" label="Term" value={months} onChange={setMonths} suffix="months"/></div><div className="grid gap-4 sm:grid-cols-3"><FinancialResult label="Monthly payment" value={`$ ${formatNumber(payment,2)}`}/><FinancialResult label="Total paid" value={`$ ${formatNumber(total,2)}`}/><FinancialResult label="Interest" value={`$ ${formatNumber(total-p,2)}`}/></div><p className="text-xs leading-5 text-muted-foreground">Assumes a fixed rate and constant monthly payments. Does not include fees, taxes, insurance, or other credit costs.</p></div>}
