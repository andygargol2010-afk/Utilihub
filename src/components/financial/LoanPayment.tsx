import{useState}from"react";import{FinancialField}from"./FinancialField";import{FinancialResult}from"./FinancialResult";import{formatNumber,parseNumber}from"@/lib/units";
export default function LoanPayment(){const[principal,setPrincipal]=useState("100000"),[rate,setRate]=useState("10"),[months,setMonths]=useState("60");const p=parseNumber(principal),r=parseNumber(rate)/100/12,n=Math.floor(parseNumber(months));if(![p,r,n].every(Number.isFinite)||p<=0||n<=0)return <div className="grid gap-4 sm:grid-cols-2"><FinancialField id="loan-p" label="Capital del préstamo" value={principal} onChange={setPrincipal} suffix="$"/><FinancialField id="loan-rate" label="Tasa anual" value={rate} onChange={setRate} suffix="%"/><FinancialField id="loan-months" label="Plazo" value={months} onChange={setMonths} suffix="meses"/></div>;const payment=r===0?p/n:p*r*Math.pow(1+r,n)/(Math.pow(1+r,n)-1),total=payment*n;return <div className="space-y-6"><div className="grid gap-4 sm:grid-cols-2"><FinancialField id="loan-p" label="Capital del préstamo" value={principal} onChange={setPrincipal} suffix="$"/><FinancialField id="loan-rate" label="Tasa anual" value={rate} onChange={setRate} suffix="%"/><FinancialField id="loan-months" label="Plazo" value={months} onChange={setMonths} suffix="meses"/></div><div className="grid gap-4 sm:grid-cols-3"><FinancialResult label="Cuota mensual" value={`$ ${formatNumber(payment,2)}`}/><FinancialResult label="Total pagado" value={`$ ${formatNumber(total,2)}`}/><FinancialResult label="Intereses" value={`$ ${formatNumber(total-p,2)}`}/></div><p className="text-xs leading-5 text-muted-foreground">Supone una tasa fija y pagos mensuales constantes. No incluye comisiones, impuestos, seguros ni otros costes del crédito.</p></div>}
