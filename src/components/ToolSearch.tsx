import{useMemo,useState}from"react";import{Search,SlidersHorizontal,Star}from"lucide-react";import{Input}from"@/components/ui/input";import{Button}from"@/components/ui/button";import{CATEGORIES,TOOLS,type CategorySlug}from"@/lib/tools";import{FINANCIAL_TOOLS}from"@/lib/financial-tools";import{ToolCard}from"@/components/ToolCard";import{useFavorites}from"@/hooks/use-favorites";
const ALL_TOOLS=[...TOOLS,...FINANCIAL_TOOLS];
function normalize(s:string){return s.normalize("NFD").replace(/[\u0300-\u036f]/g,"").toLowerCase()}
export function ToolSearch({initialCategory}:{initialCategory?:CategorySlug}){const[query,setQuery]=useState("");const[category,setCategory]=useState<string>(initialCategory??"all");const{favorites,toggle,ready}=useFavorites();const results=useMemo(()=>{const q=normalize(query.trim());return ALL_TOOLS.filter(t=>(category==="all"||t.category===category)&&(!q||normalize(`${t.name} ${t.summary} ${t.keywords.join(" ")}`).includes(q)))},[query,category]);const favTools=ready?ALL_TOOLS.filter(t=>favorites.includes(t.slug)):[];return <div className="space-y-7"><div className="rounded-2xl border border-border bg-surface/55 p-3 sm:p-4"><div className="relative"><Search className="pointer-events-none absolute left-3.5 top-1/2 size-4 -translate-y-1/2 text-muted-foreground"/><Input type="search" value={query} onChange={e=>setQuery(e.target.value)} placeholder="Buscar: porcentaje, contraseñas, interés compuesto…" aria-label="Buscar herramientas" className="h-12 border-border/70 bg-card pl-11 text-base shadow-sm"/></div><div className="mt-3 flex items-center gap-2 overflow-x-auto pb-1"><SlidersHorizontal className="size-4 shrink-0 text-muted-foreground"/><Button type="button" size="sm" variant={category==="all"?"default":"outline"} onClick={()=>setCategory("all")} className="shrink-0 rounded-lg">Todas</Button>{CATEGORIES.map(c=><Button key={c.slug} type="button" size="sm" variant={category===c.slug?"default":"outline"} onClick={()=>setCategory(c.slug)} className="shrink-0 rounded-lg">{c.name}</Button>)}<Button type="button" size="sm" variant={category==="finanzas"?"default":"outline"} onClick={()=>setCategory("finanzas")} className="shrink-0 rounded-lg">Finanzas</Button></div></div>{favTools.length>0&&!query&&<section aria-labelledby="favoritos"><div className="mb-4 flex items-center gap-2"><Star className="size-4 fill-highlight text-highlight"/><h2 id="favoritos" className="text-lg font-bold">Tus favoritos</h2></div><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{favTools.map(t=><ToolCard key={t.slug} tool={t} isFavorite onToggleFavorite={toggle}/>)}</div></section>}<div aria-live="polite">{results.length===0?<p className="rounded-2xl border border-dashed border-border bg-surface p-8 text-center text-sm text-muted-foreground">No hay herramientas que coincidan con «{query}».</p>:<div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{results.map(t=><ToolCard key={t.slug} tool={t} isFavorite={favorites.includes(t.slug)} onToggleFavorite={toggle}/>)}</div>}</div></div>}
